<local:ThemeContentDialog x:ConnectionId='1'
    x:Class="VisualExplorer.UI.Dialogs.UIMultiplyOptionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VisualExplorer.UI.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PrimaryButtonText="{x:Null}"
    SecondaryButtonText="{x:Null}"
                                                               
                          >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock x:ConnectionId='2' Name="MessageLabel" Grid.Row="0" VerticalAlignment="Top" FontSize="18"
                                               TextWrapping="Wrap"/>
        <ItemsControl x:ConnectionId='3' Name="ButtonPanel" Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Right"
                                                    >
            <ItemsControl.ItemTemplate>
                <DataTemplate                      >
                    <Button x:ConnectionId='4'                    Margin="5,0,5,0" FontSize="16"
                                                />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                </Style>
            </ItemsControl.ItemContainerStyle>
        </ItemsControl>
    </Grid>
    
</local:ThemeContentDialog>

