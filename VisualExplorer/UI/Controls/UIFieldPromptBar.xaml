<UserControl
    x:Class="VisualExplorer.UI.Controls.UIFieldPromptBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VisualExplorer.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="{x:Bind themeAgency.AppTheme, Mode=OneWay}"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Orientation="Vertical" Background="{ThemeResource DefaultSolidBackground}">
        <Button Name="PromptButton" HorizontalAlignment="Stretch" Background="Transparent"
                Content="{x:Bind PromptMessage.Content, Mode=OneWay}"
                Click="PromptButton_Click"/>
    </StackPanel>
</UserControl>
